package com.energy.app.ui.theme

import androidx.compose.ui.graphics.Color

// Oura-style near-black canvas — deep, warm-neutral, minimal chrome.
val EnergyBackground = Color(0xFF08080C)
val EnergySurface = Color(0xFF121218)
val EnergySurfaceHigh = Color(0xFF1A1A22)

// Hairline borders — Oura's signature card treatment (no heavy shadows).
val EnergyHairline = Color(0x14FFFFFF)
val EnergyHairlineStrong = Color(0x26FFFFFF)

// Accents — used sparingly, one at a time.
val EnergyOrange = Color(0xFFFF7A1A)
val EnergyCoral = Color(0xFFFF5F6D)
val EnergyMint = Color(0xFF4CD9A5)

// Text
val EnergyTextPrimary = Color(0xFFF4F4F6)
val EnergyTextSecondary = Color(0x99F4F4F6)   // ~60% — muted, Oura-like
val EnergyTextTertiary = Color(0x66F4F4F6)    // ~40%

// Ring palette (activity rings keep their three hues, softened)
val RingMove = Color(0xFFFF5F6D)
val RingExercise = Color(0xFF4CD9A5)
val RingStand = Color(0xFF54B8FF)
